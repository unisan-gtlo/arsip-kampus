services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: ${DB_NAME:-arsip}
      POSTGRES_USER: ${DB_USER:-arsip}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-arsip}
    volumes:
      - pgdata:/var/lib/postgresql/data
    ports:
      - "5433:5432"

  web:
    build: .
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      DJANGO_SETTINGS_MODULE: config.settings.dev
      DB_HOST: db
    depends_on:
      - db

volumes:
  pgdata:
